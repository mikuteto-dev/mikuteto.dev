# Chrome Origin Trial token for the "HTMLInCanvas" feature.
# Register the production origin (https://mikuteto.dev) at:
#   https://developer.chrome.com/origintrials
# Without this the Displacement hero renders as plain type, which is fine.
PUBLIC_HTML_IN_CANVAS_TOKEN=
